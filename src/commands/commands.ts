/**
 * Ribbon function file. The "Notes" button opens the task pane directly
 * (a ShowTaskpane action), so there is no custom function to run here — we only
 * need Office to initialise this page for the add-in command runtime.
 */
Office.onReady(() => {
  /* no ribbon functions to register */
})
